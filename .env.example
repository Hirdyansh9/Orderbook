# Frontend Environment Variables

# API Configuration
# Local development (adjust port if needed)
VITE_API_URL=http://localhost:5001/api

# Production Example:
# VITE_API_URL=https://api.yourdomain.com/api

# Note: Environment variables must be prefixed with VITE_ to be exposed to the frontend

